<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_hrsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".authentication.ForgotPasswordActivity"></activity>
        <activity
            android:name=".employee.ViewVacRequestActivity"
            android:theme="@style/AppTheme.PopUp" />
        <activity
            android:name=".employee.ViewLevRequestActivity"
            android:theme="@style/AppTheme.PopUp" />
        <activity android:name=".utils.SplashActivityJava">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin.VacRecyclerActivity" />
        <activity android:name=".admin.LevRecyclerActivity" />
        <activity android:name=".admin.DeleteEmployeeActivity" />
        <activity android:name=".admin.AddEmployeeActivity" />
        <activity android:name=".authentication.LoginActivity" />
        <activity android:name=".employee.RequestVacationActivity" />
        <activity android:name=".employee.RequestLeaveActivity" />
        <activity android:name=".offerselements.Hotels" />
        <activity
            android:name=".admin.MainAdminActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".employee.MainEmployeeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>